import { render, screen } from '@testing-library/react';
import renderer from 'react-test-renderer';
import App from './App';

test('renders app', () => {
  render(<App />);
});

test('adds to cart', () => {

})